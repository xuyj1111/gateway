spring:
  application:
    name: gateway
  cloud:
    config:
      label: master
      name: gateway
      profile: dev
      uri: http://localhost:8101
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka/
